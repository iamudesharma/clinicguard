# Models + native libraries are downloaded, not committed
models/
third_party/native/

# Dart/Flutter
.dart_tool/
build/
*.iml
.DS_Store

# Internal sherpa-sync tooling — never pushed
patches/
scripts/sherpa_sync.py
UPSTREAM_SYNC.md

# Example lockfiles (packages: not tracked)
example/pubspec.lock
