name: clinic_guard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^1.6.0
  http: ^1.6.0
  provider: ^6.1.5+1
  speech_to_text: ^7.4.0
  supabase_flutter: ^2.17.2
  voice_forge_flutter:
    path: ../voice_forge/packages/voice_forge_flutter
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
